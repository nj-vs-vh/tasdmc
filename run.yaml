name: test-run
append_index_if_exists: True

verbose: True

corsika_input_files:
  particle: proton
  log10E_min: 17.5
  log10E_max: 20.5
  high_E_hadronic_interactions_model: QGSJET  # QGSJET | EPOS
  low_E_hadronic_interactions_model: URQMD  # FLUKA | URQMD
  # relative to optimal number of events in each energy bin. if omitted, value is set ot 1.0
  event_number_multiplier: 1
